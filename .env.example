# API — choix selon environnement
#
# Dev full-stack local (Studio / codegen) : backend sur :3001 obligatoire
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
#
# Frontend local → API Coolify (nécessite backend prod à jour avec routes /codegen/*)
# NEXT_PUBLIC_API_URL=https://api.webplatform.kdevs.io/api/v1

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags — studio code libre (défaut) vs éditeur blocs legacy
# NEXT_PUBLIC_USE_BLOCK_EDITOR=true
